name: Gatsby Build Manually

on:
  workflow_dispatch:

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest

    steps:
      env:
        G_TAG: ${{ secrets.G_TAG }}
        OUTPUT_DIR: ${{ secrets.OUTPUT_DIR }}

    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Node.js
      uses: actions/setup-node@v2
      with:
        node-version: '20.3.1'

    - name: Install dependencies
      run: npm install

    - name: Build Gatsby App
      run: npm run build

    - name: Deploy to Hosting Service
      run: |
        # ここにデプロイのスクリプトを記述
        # 例: デプロイを行うコマンドを実行
        # この部分は、使用しているホスティングサービスに応じてカスタマイズしてください
